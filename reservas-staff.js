// reservas-staff.js - Gestión de terapeutas y disponibilidad

/**
 * Maneja los cambios en los campos relacionados con el personal/terapeutas
 * Esta función es llamada desde reservas.html cuando cambian los selectores de servicio o sala
 */
function handleStaffFieldsChange() {
    // Esta función puede estar vacía o realizar validaciones específicas
    // Se mantiene por compatibilidad con el código existente
    console.log('[STAFF] handleStaffFieldsChange called');
}

// Exportar para uso global
window.handleStaffFieldsChange = handleStaffFieldsChange;
